func main() Void {
    var x Int;
    var y Bool;
    var u Float;
    var v String;
    x + x;
    x * u;
    x - y;
    y & y;
    v + x;
    v = "haha";
    u = 3.2;
    y = false;
    x = 5;
    x % x;
    y && y;
    x ^ x;
    y ^ y;
    u * u;
    x < y;
    x == y;
    x <= y;
    y >= x;
    y > y;
    return ;
}